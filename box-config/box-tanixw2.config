#  SPDX-License-Identifier:	GPL-2.0+
#  Version 2022 by Devmfc
#
#	Box: Tanix W2 S905W2- Amlogic S4 (S905W2/S805X2/S905Y4) - 2GB - 100M - HDMI via simplefb
#
# Comments are only allowed at the beginning of a line

dtb_img=dtb-${release}/amlogic/meson-s4-tanixw2.dtb

load_config0=s4-default-${variant}.config

load_config5=s4-debug-${variant}-${debug}.config

# eMMC Amlogic android partitions (may vary by android image version!), enable this when you want to access the Amlogic partitions om eMMC
#bootargs9=blkdevparts=mmcblk1:0x2000000@0x018c00000(oem),0x030e400000@0x09d400000(rootfs)

# warning, make sure this file ends with a newline!
