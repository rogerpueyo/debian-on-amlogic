#  SPDX-License-Identifier:	GPL-2.0+
#  Version 2022 by Devmfc
#
#	Box: Generic Amlogic GXM (S912) - 2GB - 1000M - qca9377 (based on q200)
#
# Comments are only allowed at the beginning of a line

dtb_img=dtb-${release}/amlogic/meson-gxm-h96proplus.dtb

os_addr=0x2000000
dtb_addr=0x4000000

#load_config0=gx-default-${variant}.config

# reset gigabit ethernet phy
run_cmd0=gpio clear gpioz_14; sleep 0.5; gpio set gpioz_14; echo 'reset phy ok'

# debug=y
load_config5=gx-debug-${variant}-${debug}.config
#bootargs0=dwmac-meson8b.cali_val=0x20000

# warning, make sure this file ends with a newline!
